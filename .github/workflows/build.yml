name: Build Android APK/AAB

on:
  push:
    # اجرا هنگام Push به شاخه‌های اصلی
    branches: [ main, master ]
  pull_request:
    # اجرا هنگام Pull Request به شاخه‌های اصلی
    branches: [ main, master ]
  workflow_dispatch:  # اجازه اجرای دستی

jobs:
  build:
    # استفاده از آخرین نسخه اوبونتو
    runs-on: ubuntu-latest
    # تنظیمات محیطی برای P4A/Buildozer
    env:
      # افزایش زمان تایم‌اوت پایتون برای پکیج‌های سنگین
      PYTHON_FOR_ANDROID_THREAD_TIMEOUT: 600
      # جلوگیری از درخواست‌های مکرر در صورت خطا
      PYTHON_FOR_ANDROID_IGNORE_FATAL_ERRORS: 1
      
    steps:
    - name: 📝 Checkout code
      uses: actions/checkout@v4 

    - name: 📦 Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: 🛠️ Install dependencies for Buildozer & Cython
      # Cython برای کامپایل Kivy و Pyjnius مورد نیاز است.
      run: |
        sudo apt-get update
        # نصب پکیج‌های سیستمی مورد نیاز
        sudo apt-get install -y git zip unzip openjdk-17-jdk python3-pip autoconf libtool pkg-config zlib1g-dev libncursesw5-dev cmake
        # نصب Cython و Buildozer (Cython باید قبل از Buildozer نصب شود)
        pip install cython buildozer

    - name: ⚙️ Set execution permissions
      run: chmod +x buildozer.spec

    - name: 🔨 Run Buildozer (Debug Build)
      run: buildozer android debug
      
    - name: ⬆️ Upload APK artifact
      uses: actions/upload-artifact@v4 
      with:
        name: ${{ github.event.repository.name }}-${{ github.run_number }}-apk
        path: bin/*.apk
        
    - name: 📦 Create GitHub Release
      if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
      uses: softprops/action-gh-release@v1
      with:
        tag_name: v${{ github.run_number }}
        name: Release v${{ github.run_number }}
        files: bin/*.apk
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
